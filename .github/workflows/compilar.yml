name: Compilar y Pruebas C#
on: push
jobs:
  Compilacion:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Compilamos
        run: dotnet build
      - name: Ejecutamos las pruebas
        run: dotnet test